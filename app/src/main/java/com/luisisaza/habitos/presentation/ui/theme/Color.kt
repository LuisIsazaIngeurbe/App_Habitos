package com.luisisaza.habitos.presentation.ui.theme

import androidx.compose.ui.graphics.Color

// ───────────────────────────────────────────────────────────────────
// BRAND PALETTE — modify these to change the entire app theme
// ───────────────────────────────────────────────────────────────────
val PaletteOlive = Color(0xFF6E7D4E)    // Primary
val PaletteSage = Color(0xFFD8E0C7)     // Primary container / accent surfaces
val PaletteWhite = Color(0xFFFFFFFF)    // Background / surface
val PaletteCopper = Color(0xFFAA7229)   // Secondary
val PalettePeach = Color(0xFFF0C48C)    // Secondary container
val PaletteCream = Color(0xFFFAF7F0)    // Soft surface

// Derived shades (kept consistent with palette)
val PaletteOliveDark = Color(0xFF4F5A38)
val PaletteSageDark = Color(0xFFB6C19E)
val PaletteCharcoal = Color(0xFF2B2A26)
val PaletteSoftCharcoal = Color(0xFF3F3D38)
val PaletteWarmGray = Color(0xFF8D8478)
val PaletteOutline = Color(0xFFE0DDD3)

// ───────────────────────────────────────────────────────────────────
// FIXED SEMANTIC COLORS — these MUST NOT change with palette
// (good/bad habit indicators + compliance status)
// ───────────────────────────────────────────────────────────────────
val HabitGoodIcon = Color(0xFF4CAF50)
val HabitGoodContainer = Color(0xFFC8E6C9)
val HabitBadIcon = Color(0xFFE53935)
val HabitBadContainer = Color(0xFFFFCDD2)

val ComplianceGreen = Color(0xFF4CAF50)
val ComplianceYellow = Color(0xFFFFC107)
val ComplianceRed = Color(0xFFF44336)
val ComplianceGray = Color(0xFFE0E0E0)

// Generic feedback (used by Material errorContainer)
val ErrorRed = Color(0xFFC62828)

// Dark theme surfaces
val DarkSurface = Color(0xFF1F2421)
val DarkBackground = Color(0xFF161A18)
val DarkSurfaceVariant = Color(0xFF2A302C)
